# chaoxing drift register

| Risk | Evidence | Required response |
|---|---|---|
| Browser-bound `uf` Cookie blocks reqwest | 2026 agent donor explicitly uses same-origin Browser fetch | Compare Native HTTP and Browser fetch with the same sanitized live case; select transport per capability |
| Native QR route or state codes drift | The only complete request chain is the pinned 2024 CxKitty implementation | Keep the advertised QR method at Development until a sanitized live run proves login/create/poll/Course validation; unknown states and redirects fail closed, and BrowserBridge remains the fallback |
| Process-local QR state is mistaken for a durable auth workflow | A successful status poll can precede credential commit and a restart can occur between them | Encode the exact Provider state as an encrypted account/AuthSession/correlation-bound continuation, CAS every poll revision, persist a success terminal before deterministic finalization, and atomically commit that terminal with AuthSession/Credential state; never replay the remote poll during finalization |
| `enc` expires or changes | Current Work routes require a fresh course/session value | Never persist `enc` as identity; reacquire it before Work scanning |
| Mobile API behavior is stale | CxKitty implementation revision is from 2024 | Treat all `mooc1-api` mobile paths as unverified fallbacks |
| Domain variants change | Donors use `mooc1`, `mooc1-api`, `mooc2-ans` and historical institutional mirrors | Allow only audited hosts and classify unexpected redirects as protocol drift |
| Status keyword false positives | Exam pages embed localized status words in scripts | Remove script/style content and parse item structure before fallback text classification |
| Exam score/retake false positives | The PortSource scans localized score text and structural `reTest(...)` handlers beside remaining-time text | Bound scores to 0-100, reject conflicts/malformed precision, exclude `分钟`, require an exact row-local handler, and keep both facts separate from state and authorization |
| Exam score is mistaken for answer proof | Result score and visible per-Question answers are independent donor facts | Preserve score as exact thousandths on any verification status, but derive Question confirmation only from selected bound ID/original-position/type/value evidence after validating the complete v3-bound result QID/order set |
| Exam result route or facts drift | Current PortSource distinguishes Exam preview/detail pages, but entry variants and result DOM may differ | Read only the allowlisted preview path with exact course/class/exam binding, reject redirects and conflicting list/detail scores, and retain unsupported variants as list-level evidence until separately fixtured |
| `未交` is ambiguous | Current donor observed expired Work still labelled `未交` | Follow the detail redirect before declaring a task actionable |
| Per-attempt QIDs change | Current Exam donor observed complete QID regeneration on retake | Reparse every attempt; never make QID the task identity |
| Browser DOM handlers change | OCS and agent donors use different current page modes | Fixture each supported page mode and fail closed on unknown form structure |
| Formal assessment ambiguity | Exam is a source module, not an assessment nature | Keep source classification separate from the explicit assessment facts used by product authorization; do not remove evidenced Provider capabilities |
| Card version or execution token changes | Primary donor currently sends card version `2025-0424-1038-3`; immediate resources use `jtoken`, while Video uses `objectId` plus a status `dtoken` and MD5 report signature | Re-fetch before every execution, never persist token/version-derived routes, and require fresh-card completion verification |
| Audio is guessed after a Video failure | Samueli does not retain the media module and retries Audio after an undifferentiated Video failure, while current OCS cards expose `property.module=insertaudio|insertvideo` | Classify Audio only from a fresh exact `insertaudio` module, select the Audio Referer and `dtype=Audio` before dispatch, and never switch media kind after a request failure |
| Work final-submit form changes | The primary donor copies current editor fields, while older mobile donors name a fixed but different parameter set | Parse a fresh editor, forward only the audited bounded allowlist, bind course/class and the exact Question/type set, and fail closed if required attempt material changes |
| Work acknowledgement is mistaken for completion | Donors log `status=true` as success, while current task pages may remain on prompt/waiting states | Persist only a Receipt, then independently re-discover the Work; prompt/editor remain Pending and only exact server-visible result answers confirm completion |
| Work/Exam result text is normalized into a false answer match | Filtering arbitrary text down to uppercase letters can turn `not-B` into `B`, deduplication can hide repeated choices, substring checks can classify `不正确` as true, and truncating at a second `我的答案` can hide a conflicting later value. Conversely, controlled `未作答/待批阅/暂无答案` placeholders are missing evidence rather than malformed protocol | Require exactly one visible-answer label per selected result node, accept only uppercase option IDs separated by audited punctuation, reject duplicate or multi-valued single-choice results, and map true/false only from complete controlled tokens; malformed visible values are protocol drift, while controlled empty/pending placeholders make verification Inconclusive and leave Draft Questions Unverified |
| Result Question identity is repaired before binding | Trimming a Chapter `data` or Exam `questionId.value` attribute can turn a padded/changed remote identity into the Draft QID | Validate the raw DOM attribute directly with the bounded QID grammar; whitespace-padded identities are ProtocolDrift and are never rebound by normalization |
| Chapter Work completed card is mistaken for answer proof | Primary donor exposes only `isPassed`, while the 2026 Chapter-test donor reaches visible answers and final score through a separate `selectWorkQuestionYiPiYue` iframe | Keep card completion task-level; require a freshly bound iframe document, complete QID/order/type/value facts, and the strict Provider parser before emitting per-Question verification |
| Chapter Work standard answers are reused across retakes | The Chapter-test donor reports that option `data` values are randomized after `redoTest` while displayed letters remain stable | Parse standards only from the fresh bound result, require every value to match the current Question option IDs, and never cache or execute a later retake from an earlier mapping |
| External or guessed answers are mislabeled Provider-native | Samueli uses configured Tiku plus random fallback; CxKitty uses REST/JSON/SQLite/OpenAI plus fuzzer; OCS uses configurable wrappers/cache; the agent skill consumes pre-computed answers | Keep those sources in Core external/manual evidence contracts; Chaoxing AnswerResolve accepts only a fresh completed platform result's `正确答案`, and remains unregistered until its post-result lifecycle is explicit |
| Post-result standards are used as pre-submit authority | The only native standard-answer evidence appears after a completed Chapter Work result, while normal AnswerResolve precedes Draft construction | Keep the typed component unregistered; require fresh completed TaskDetail and bound result transport, and add an explicit Main lifecycle before exposing the capability |
| Result-page answer DOM changes | OCS and browser donors distinguish `.mark_answer`/`我的答案` from editable hidden inputs and CSS selection state; Exam retakes rotate QIDs | Require an allowlisted Work view or strictly bound Exam preview and complete unique QID set; selected Questions additionally require original DOM position, exact type and bounded visible-answer grammar. Missing/extra/drifted selected facts stay Inconclusive and duplicates fail closed instead of inferring from unselected controls, CSS, score or list text |
| Exam start response is ambiguous | `phone/start` creates an attempt before dynamic `enc` Question acquisition completes | Persist the exact start request digest before send, never auto-replay an issued operation, and add only fresh readback-based recovery when live evidence proves it |
| Exam-code gate is inferred from a substring or missing declaration | CxKitty extracts decimal `needcode` and converts any nonzero integer to true; checking only whether the statement contains digit `1` misclassifies `2`, while treating a missing/malformed declaration as false can bypass a gate | Require exactly one `var *needcode *= *(\d+);` declaration, parse a bounded decimal, map only zero to false, and fail closed on missing, duplicate, quoted, fractional or lookalike declarations |
| Face/captcha gates are defaulted false from missing or ambiguous inputs | The cover exposes `faceRecognitionCompare` and `captchaCheck`; selecting only the first match and using `Option::is_some_and` turns a missing field into false and ignores duplicates | Require one exact hidden input for each gate and accept only raw `0` or `1`; missing, duplicate, padded or other values are ProtocolDrift before Exam start |
| Exam preview rotates attempt state | CxKitty overwrites `enc`, remaining time and last-update fields from the full preview before saving answers | Parse and bind preview state after start; reject any save response which regresses time or advances more than one Draft cursor |
| Exam answer/final POST is ambiguous | Temporary saves and final submit share a non-idempotent route with randomized signatures | Freeze signature/query/form and digest before send; never replay a temporary save; recover only a final submit when fresh exact Exam inventory is Completed |
| Coverage is computed from selected/supported Questions only | Samueli defaults `cover_rate` to 0.9 and OCS divides resolved Questions by all Questions | Core binds the complete snapshot denominator, threshold and unanswered IDs; Work/Chapter Work require the same fresh editor count and every selected QID/type, preserve all controls in DOM order, and emit empty unanswered values. Mobile Exam v3 preserves the complete ordered binding and freezes selected original positions; unknown/unsupported unselected types lower coverage and never receive invented values |
| Partial Exam cursor uses selected-list indexes | CxKitty sends `start` as the current full-paper Question index while a partial Draft omits unanswered Questions | Persist the complete ordered QID/position/content-fingerprint binding and frozen selected positions in encrypted v3 state; emit original position minus one and advance only the selected cursor |
| Save-only is mistaken for final submit | Samueli separates `submit=false`; OCS exposes `save`, `nomove`, numeric thresholds and `force` | Model mutation intent separately and never turn save-only or a prerequisite unlock into an implicit final submit |
| Bootstrap creates a new attempt | Work `api/work`, Exam `phone/start`, Chapter `redoTest` and Exam `reTest` can mutate remote state | Historical bootstrap may only enumerate completed rows and strictly bound read-only result routes; fail closed before any attempt-creating call |
| Completion/score is mistaken for answer evidence | Donors expose completed rows and scores even when visible answers are absent or fill/short grading is pending | Keep state, score, submitted value, standard/reference answer and per-Question correctness as separate evidence facts |
| Score improvement silently retakes | Agent donor has bounded pass/target/retry policy and retakes rotate QIDs | Use independent Strict Completion and Score Improvement state machines; require explicit fresh retake authority, snapshot, Draft and durable operation |
| Type 10 is normalized uniformly | OCS currently treats code 10 as completion while existing Asterism evidence maps it Composite | Preserve route/type provenance and block submission until a route-specific sanitized fixture resolves the conflict |
| Shared-option/composite Questions are flattened | OCS types 14/15 expose children and issue #297 reports a shared-option shape | Add shared Domain context/options/children; do not store an opaque answer string or reuse option letters across attempts |
| Fill/short result text is assumed uniquely correct | `chaoxing-exam` uses editors and batch evidence reports pending manual grading | Accept only a Question-bound single blank with one exact visible non-placeholder value; keep multi-blank, short-answer and pending/manual-grading states unsupported until separate exact DOM/reference fixtures exist |
| Independent Work fill inherits Chapter/Exam semantics without a page binding | The pinned primary donor uses one `answer{qid}` field for type 2 and may concatenate multiple returned blanks, while the current Work parser can count rendered blanks | Permit only `blank_count=1` plus one Draft text and exact `/work/view` visible-value readback; reject multi-blank concatenation and keep independent short answers unsupported until their grading/result semantics are evidenced |
| Synthetic history transport is mistaken for live support | The audited Chapter result is iframe-bound and no current Native list/read endpoint is established | Keep `AnswerHistoryHarvest` absent from the Native factory until a real BrowserBridge/Capture transport proves pagination, `workAnswerId` binding and read-only result access |
| Low-level sign-in methods, frozen queries or Receipts are mistaken for an automated capability | Current sign-in donors corroborate read-only `activelist`/`signDetail` and overlapping ordinary fields but not one complete ordinary sequence: Ylim uses its own `preSign -> stuSignajax`, Samueli adds different fields, and `mini-hbut` uses the broader pre-sign shape before a different `/pptSign` route. Ylim also calls dynamic `analysis`/`analysis2` even though its source comment labels that pair location-required. The donors further disagree on `status=1`, `otherId=5` and non-normal prerequisites, while neither `success` nor pre-sign `签到成功` is an independent fresh account-result readback | Keep the reader, explicitly family-tagged exact Ylim ordinary preparation, separately bound donor-specific analysis continuation and pure Receipt parser unregistered and transport-free. Never splice one donor's pre-sign query into another donor's terminal route. Treat `preSign` as the first issued step of one durable non-idempotent sequence, never as a free read; do not infer that Ylim's unconditional implementation proves universal ordinary-sign necessity, retain conflicted codes without completion inference, and require explicit variant inputs plus an independent account sign-result readback before advertising mutation |
| Scanned QR material or a frozen second-step query is treated as mutation authority | Ylim exposes a dynamic `SIGNIN:` refresh payload and exact `stuSignajax` fields but leaves QR unwired, while `mini-hbut` parses several Chaoxing URL hosts and treats a different `preSign` shape as terminal. Mixing `aid` with `activeId|id`, or carrying Ylim-only `Code/source` on an HTTPS URL, can hide a conflicting activity or donor family | Require one identity alias globally, enforce carrier-specific aliases/fields and bind only one allowlisted carrier to a fresh exact QR activity; keep `enc`/code/query zeroizing and expose digests only. A frozen Ylim submit step never authorizes skipping or guessing `preSign`; do not send either donor sequence until durable step authority and independent account-result verification exist |
| A WebIM event or donor-local history row is treated as remote completion | Ylim listens for `att_chat_course.atype=2`, forwards SDK messages without an event-ID/cursor or de-dup ledger, recursively reconnects on close, then writes its own Prisma `SignLog` after calling the opaque sign endpoint; `sign_history` reads only those local rows | Use WebIM only as course/class/activity discovery requiring fresh `signDetail` rebinding. Never ingest local `SignLog` as Provider evidence; require an independent Chaoxing account-result read and durable event/subscription/mutation authority with explicit ordering/recovery |
| Learning-count local loops are mistaken for verified remote progress | Samueli commit `38a2698` increments a process-local counter even when `setlog` is absent/rejected or monitor requests fail, and exposes no server count read | Do not execute or report count progress until a server-visible readback exists; ledger every issued `setlog` and never replay an ambiguous visit |
| Browser click/wait is mistaken for hyperlink or timed-read completion | OCS clicks `#hyperlink` or drives a timed nested reader and then waits, without a completion readback | Bind the exact frame/attachment/job in a versioned BrowserBridge command and accept success only after a separate fresh card/progress read proves completion |
| Random in-video answers become accepted evidence | OCS exposes only random or ignore strategies for video questions | Retain the subflow in scope but reject random answers; require exact Question identity, an authorized candidate and server-visible saved-result evidence |
| Generic incomplete state is over-diagnosed | `NotOpen` and Pending/InProgress collapse multiple donor causes, while only Expired is an exact closed-window fact | Map only fresh Expired verification to `WindowClosed`; leave the other states on Core's conservative `RemoteUnknown` path until result provenance distinguishes prerequisite, teacher review, duration or human action |
| Protocol observation leaks response content | Unknown type/result branches can contain DOM text, answer values, route identities or ephemeral fields even when the final error message is sanitized | Observe only numeric codes, controlled enums, bounded counts and static field-presence booleans; collapse unknown strings to `other`/presence and leave branches unobserved when no safe shape is already available |

## Live-validation gate

Before `VerificationLevel` can advance:

1. validate password login, stored Cookie reuse, expiry detection and recovery;
2. scan a real course containing independent Work and Exam entries;
3. compare Native HTTP facts with the visible platform and Browser same-origin
   results;
4. record open/due/close behavior for at least one pending and one closed task;
5. verify one allowed non-formal submission through a remote result readback;
6. force or observe auth expiry, rate limiting and one protocol/error branch;
7. save only sanitized fixtures and a dated verification record.
