# WELearn drift register

| Risk | Static evidence | Required response |
|---|---|---|
| OIDC authorize/return shape changes | Current donor reaches exact `/idsvr/transfer.html`, extracts one `/connect/authorize/callback?...` `rturl`, then receives exact `/idsvr/connect/authorize/callback?...` after login; older donors construct/extract the route differently | Bind the current transfer and callback paths exactly, reject another path even on the trusted SSO host, and add a sanitized fixture before widening |
| OIDC introduces another trusted host or redirect count grows | The audited flow currently stays on two hosts and completes within a short chain | Fail closed at the host/redirect bound; expand only from sanitized current evidence |
| SSO Cookie domain/path layout changes | Current native boundary accepts only three audited domains and applies path scoping | Treat rejected scope or missing authenticated Course read as protocol drift, never relax from an error body alone |
| Course path alternates between `/student` and `/2019/student` | Donors mix both paths | Treat one as current only after live comparison; do not scatter hard-coded paths |
| `courseunits` accepts GET and POST in different donors | The current donor uses POST while YZBRH and Auto_WeLearn use GET | Prefer POST and retry the read-only GET variant only after an explicit 404/405; never broaden fallback to authentication, network or parser errors |
| Completion labels are localized or schema-dependent | `scoLeaves` donors inspect text fields directly | Keep raw sanitized label and map unknown values to `Unknown` |
| Course/Unit/SCO inventory envelopes rename or retype their row arrays | Audited Course responses use `clist`; Unit and SCO-leaves responses use `info`, all as arrays | Fail closed and attach only fixed document/field labels plus root/field JSON type categories as `FieldDrift`; never include field values, unknown members or response text |
| SCO module label is mistaken for assessment nature | Current inventory exposes Resource-like leaves but no formal/routine classification field | Keep assessment class `Unknown`; classify only from a separately audited explicit fact |
| `getscoinfo_v7` outer or nested shape changes | Audited response stores JSON text inside the outer `comment` string, reports integer `ret=0` and may omit `cmi`; YZBRH starts only after raw response text contains exact `学习数据不正确`, but otherwise synthesizes empty preservation defaults when `cmi` is absent | Keep both JSON layers bounded for reads. Attach only outer root/result JSON types and a bounded result-state category when the outer object or `ret` drifts; never include the result value or nested comment. The CMI reader may retain the exact uninitialized marker despite an unexpected media type only after rejecting structural login HTML. PreserveFresh starts only for the marker; valid no-CMI fails before mutation instead of synthesizing fields. ResourceExecution may use absent baseline only for zero-time/save-only, read capabilities never start, and every post-start CMI remains strict |
| Post-start or verification CMI omits preservation fields | Duration finalization requires explicit completion/progress/score/success and time observations | Stop before heartbeat/finalization when the baseline is incomplete; reject incomplete readback instead of comparing synthesized defaults |
| CMI progress grammar changes | Current donor reads decimal `progress_measure` in the `0..1` range | Accept only the bounded audited decimal grammar; add sanitized evidence before widening it |
| CMI time units/format drift | Current donors advance integer `session_time`/`total_time` against real seconds, but live variants may differ | DurationRead accepts only bounded canonical integer seconds and fails closed on every other grammar; widen only from sanitized evidence |
| Heartbeat semantics drift | Current Fanyuchang sends the long action every second; YZBRH and Auto send network keeps every 60 seconds | Expose only the evidenced `{1,60}` Master-controlled values and bind client-counter to 1 while preserve-fresh/implicit-server require 60; widen only from sanitized evidence |
| A restored Duration plan bypasses runtime cadence validation | Current client-counter uses one-second keeps while preserve-fresh and implicit-server use 60 seconds, all within the evidenced bounded target range | Own one public Duration-plan validator and invoke it before fresh detail and again at native transport entry so invalid plans perform no I/O |
| Distinct duration donors collapse into one heartbeat payload | YZBRH sends an initial preserved-state keep and preserve save; current Fanyuchang sends per-second client counters; Auto waits before implicit keeps and emits a bare save | Keep three explicit wire modes, bind current/Auto to their evidenced intervals, and fresh-read the same preservation predicate after every mode |
| Start/keep/save result grammar changes | Audited donors treat start/save `ret=0` and keep `ret=0/1` as accepted | Fail closed on missing or non-integer `ret` and attach only fixed mutation/result labels plus JSON type/state categories; retain that validated shape when a started mutation is reclassified to typed non-retryable HumanRequired, without copying the value or weakening no replay. Well-formed other integers remain explicit negative receipts; widen only from sanitized current evidence |
| Distinct donor mutation forms collapse into one request | Current Fanyuchang uses the query-uid endpoint, full start identity and the simple Referer across the lifecycle; YZBRH/Auto use the plain endpoint, minimal start identity and their exercise paths use a task-specific Referer | Keep the two ResourceExecution profiles explicit, infer the exact duration endpoint/start payload from its wire mode, and fixture both bounded endpoint/field sets |
| Session expires or a network result becomes uncertain after a duration mutation | Replaying start/keep/save could double-report time | Provider never renews-and-replays a mutation; Engine sends every only-DurationReport error or abandoned recovery directly to HumanRequired without retrying or consulting completion progress |
| Completion/progress/score can be changed independently of duration | All pinned donors implement direct `setscoinfo` and/or save payloads | Keep it as independent ResourceExecution; fresh-rebind, freeze selected score, never replay writes, and require exact fresh CMI verification |
| Historical CMI envelope marker is silently discarded | Current Fanyuchang sends plain JSON; YZBRH and Auto_WeLearn append `[INTERACTIONINFO]` to `setscoinfo.data` | Keep both formats explicit and fixture the exact suffix; do not infer or strip it from unrelated response parsing |
| Auto duration completion is forced through `setscoinfo` | Auto_WeLearn's duration worker uses only start/keeps/bare save or start/keeps/completed save, unlike its exercise worker | Keep `set_then_save` and `save_only` explicit; the modular completed-save route must execute inside the atomic duration-completion boundary and end with exact fresh CMI verification |
| One explicit negative mutation receipt suppresses later donor writes | Completion donors send later independent save calls even when start/set returns a well-formed non-success result | Record acceptance per call and continue only after an unambiguous integer result; any malformed/network/auth ambiguity stops with no replay, and only fresh exact CMI proves success |
| Historical duration receipt is treated as a hard success gate | YZBRH and Auto_WeLearn ignore well-formed negative start/keep/save results and continue, while current Fanyuchang gates start and stops its keep loop | Preserve the selected donor control flow, record accepted/rejected receipt counts, stop every ambiguous result, and require the same strict fresh CMI verification regardless of receipt |
| Duration transport receipts describe an impossible lifecycle | Preserve-fresh has an initial keep and final save, implicit-server has complete-interval keeps and a final save, while client-counter has per-second keeps, stops on first rejection and has no final save | Validate start/keep/final receipt slots and counts against the frozen protocol before CMI parsing while retaining each present boolean as diagnostics |
| Resource transport receipts describe a different mutation shape than the frozen plan | Current standalone completion has start, set and two saves; legacy set/save has one save; Auto's modular final omits set but still has one save | Validate receipt slot presence and count against the immutable plan before parsing readback; retain each present boolean as diagnostics rather than a success gate |
| Historical duration request gaps or fields are replaced by the current donor form | YZBRH sleeps two seconds around baseline/start/first keep/final save and omits `timelimitsec/endcaltime`; current Fanyuchang sends those two fields in its per-second keeps | Keep fixed YZBRH request delays outside reported duration, bind keep fields to the selected mode, and expose sanitized delay/receipt metadata |
| Current donor's second save is treated as redundant and omitted | Fanyuchang always sends selected score and then another `crate=100` save; YZBRH/Auto stop after selected score | Keep both sequences explicit. The current-donor sequence verifies final score 100, and an ambiguous second save is never replayed |
| Completion erases a preceding duration report | Normal donor completion writes zero time, while current Fanyuchang's duration-then-completion flow carries its accumulated `session_time`/`total_time` into `setscoinfo` | Keep zero-time and preserve-fresh-time modes explicit. Fresh-time mode requires both baseline values before mutation and compares them with the immediate readback |
| Recovery accidentally reduces the frozen goal to generic Completed | Score is Provider-specific and absent from generic `RemoteProgress` | Use goal-bound `TaskExecutionCapability::verify_execution` with the frozen settings; rebind Task identity and fresh-read exact completion/progress/score without entering any mutation path |
| A composite Provider call loses the fresh-time completion facts | Current Fanyuchang carries accumulated `session_time`/`total_time` into final `setscoinfo` | Keep the fresh-time setting explicit, but do not use immutable plan context as mutation authority; the atomic Core boundary must carry those values within the one donor operation and the pure verifier must compare post-duration and final time fields exactly |
| Atomic donor duration-completion is approximated by two singleton sessions | Current Fanyuchang starts once before client-counter keeps and final set/save; modular Auto starts once before implicit keeps and its completion-bearing save | Provider freezes `AtomicDurationCompletion` and both capability requirements, rejects them at singleton TaskExecution, and keeps its one-start native transport unregistered until Core can persist/authorize/recover the composite operation; never insert Auto's bare legacy save or a second start between phases |
| A prepared atomic child reaches native mutation without a fresh exact Task rebind or final verifier | Parent planning evidence can become stale before execution, and transport receipts alone do not prove completed/progress/profile-score or current time preservation | Route every prepared child through the unregistered high-level atomic coordinator: revalidate batch/child, fresh-read and rebind TaskDetail, call transport once, then apply the pure exact verifier; any fresh drift stops before transport |
| Atomic final mutation reuses the wrong standalone completion profile | Fanyuchang duration performs fresh-time set plus one score-100 save, while its standalone completion may do selected-score plus a second 100 save; modular Auto uses one zero-time score-0 save with no set | Freeze the exact `WellearnAtomicCompletionProfile` with the batch flow, derive the full immutable `WellearnAtomicDurationCompletionPlan` from that profile plus the target, and revalidate it before use; the plan itself grants no mutation authority |
| An atomic child payload is synthesized from a singleton flow, rebound to another SCO or loses an evidenced zero target | Atomic authority comes from one exact validated Fanyuchang/Auto batch entry; current Fanyuchang needs a separately frozen deterministic target and accepts literal zero, while modular Auto already stores each equal-floor target and may also store zero | Materialize a versioned bounded `WellearnAtomicChildPlan` only after full batch validation, bind Course/Task/entry ordinal plus exact flow/shape/profile/target, require an explicit bounded Fanyuchang target including zero, consume only Auto's entry target, and revalidate every restored field after decoding |
| A generic Provider plan artifact bypasses WELearn namespace or child rebinding | Core persists credential-free Provider-private JSON with a 64 KiB generic ceiling, while WELearn's atomic child payload is bounded to 1 KiB and remains meaningful only for one batch ordinal and target authority | Use only `welearn.atomic-child.v1`, reject foreign provider/type before payload decoding, retain the stricter local size/schema checks, and restore only through full batch + expected ordinal + frozen target rebinding rather than plain deserialization |
| Process recovery reconstructs a prepared child from the artifact alone | The child artifact does not contain the complete frozen batch membership or external Fanyuchang target authority, and it never grants execution authority | Rebuild the prepared coordinator input only from the independently durable complete batch, expected ordinal, frozen target authority and exact namespaced artifact; revalidate them together, then require a fresh TaskDetail rebind before transport |
| A single-Task planning hook guesses parent Unit selection or independently recomputes Auto membership | One Task reveals neither all-versus-explicit ordered Unit selection nor sibling count/order; Auto samples one aggregate before equal-floor division and current Fanyuchang needs one concrete frozen child target | Require explicit parent Course/flow/selection/expected-child/target authority, rebuild once from a complete fresh Unit/SCO inventory, then persist parent plan and all child artifacts atomically; do not override the current single-Task hook until Core carries that authority and call order |
| Restored parent authority loses Unit order or Auto budget provenance | Parent selection and one aggregate sample precede membership allocation; a raw final minute value cannot prove its configured base/range/offset | Encode only bounded v1 parent authority, retain all donor inputs, deny unknown/version/derived-value drift and re-enter the strict constructor on decode. Core must still bind the opaque bytes to one durable parent attempt before child creation |
| Atomic result concatenates incompatible singleton evidence or impossible receipts | Current needs an intermediate fresh-time CMI and can stop keeps on one rejection before final set/save; Auto has no intermediate CMI/set and emits one keep per complete minute before one completion save | Use a separate combined documents/receipt shape, retain heartbeat order, validate every slot/count against the atomic plan before CMI parsing, and keep explicit negative receipts diagnostic rather than treating them as final verification |
| Atomic start follows an unknown initial CMI document | Both atomic donors always start their selected SCO, but that behavior is not evidence that a malformed or drifted CMI response is safe to ignore | Before durable issue/start, require initialized CMI, explicit no-CMI or the exact audited uninitialized marker; repeat the check on the returned evidence bundle and fail closed without mutation for every other shape |
| Durable mutation sink leaks Core ownership, hashes an ambiguous request identity or treats an old issue as permission to resend | The Provider owns exact WELearn endpoint/Referer/form order, while Core owns Execution/attempt/worker binding and an issued row without receipt is ambiguous | Canonically domain-separate and length-prefix the Cookie-free Provider request/response identity, pass only bounded operation/ordinal/digests/acceptance through Core's generic sink, keep Core identities in the adapter, fail before mutation when the sink is absent, and require duplicate issue to fail closed; native transport persists issue before send and receipt before the next ordinal |
| Final compound verification is attached to every mutation or a rejected save | Fresh final CMI proves the compound completion goal after the ordered lifecycle; it does not prove each start/keep/set independently, and Core verification requires an accepted receipt | Derive the exact final save ordinal from the validated receipt shape, hash only the frozen goal and fresh proof documents under a separate domain, and persist `verified=true` only when that final save receipt was accepted; missing/failed persistence after mutation is HumanRequired without replay |
| Immediate and recovery paths independently rebuild the final verification record | Repeating the acceptance/ordinal/digest mapping in callers can attach proof to a rejected save or a different mutation | Use the Provider verification value's single Core-record adapter. It emits the exact final-save ordinal and observation digest only when that receipt was accepted; explicit rejection returns no record |
| Recovery after Fanyuchang final save checks only completion/score or loses the post-duration time pair | The final CMI must preserve the exact time values read between the conditional keep phase and dependent set; a later fresh read alone cannot establish that relation | Persist the bounded hash-only pre-final observation against the attempt before set issue, then use the exact receipt sequence and a read-only final CMI to re-prove time equality and the actual final-save ordinal. Missing evidence is HumanRequired; never resume or replay set/save |
| Atomic duration-completion is forced into a fixed ordinal DAG | Fanyuchang may stop counter keeps on the first explicit rejection, so the dependent set/save ordinals vary; its set request digest also depends on the post-duration CMI read | Freeze an artifact-bound conditional sequence: response-dependent bounded keeps, phase-3 pre-final observation before set, and contiguous dynamic ordinals. Auto retains its deterministic floor keep count. Reject conflicting preparation or out-of-phase issue, and never treat the ledger as replay authority |
| Recovery trusts receipt booleans detached from the frozen sequence | Generic receipts carry ordinal/digest/acceptance but not WELearn operation identity, and a shape-compatible record from another child or attempt is not recovery proof | Require the exact child-bound sequence plan plus paired contiguous issues/receipts and exact donor operation order before adapting acceptance bits. Core must load all records from the same attempt; Provider performs read-only verification and never resumes mutations |
| Atomic recovery reads final CMI before rebinding the child or through the wrong donor profile | Durable evidence can outlive Course/SCO membership, visibility or capability facts, and current Fanyuchang versus Auto use distinct endpoint/Referer profiles | Validate records first, fresh-rebind the complete prepared child, then derive the exact current/legacy read-only profile from that child. Stop before CMI on fresh drift and expose no mutation method on the recovery coordinator |
| Independently resolved Resource settings create a cross-donor mutation | Endpoint/Referer, CMI envelope, time mode, write family, score and save sequence occur only in four complete audited standalone or atomic-final profiles | Validate the complete immutable Resource plan before TaskDetail and again at native transport entry; reject every mixed profile as `Internal` without I/O |
| Hidden SCO capability is discarded because inventory reports NotOpen | Current Fanyuchang explicitly disables its visibility skip and attempts every returned SCO | Preserve NotOpen as the remote observation while advertising the donor-evidenced mutation capabilities; fresh-rebind the exact Course/SCO and grant only exact ResourceExecution/DurationReport action sets a shared state exception |
| WELearn concurrency is capped below the donor control | Auto_WeLearn exposes 1–100 workers and applies the selected bound to its thread pool | Keep Provider/account settings bounded at 100 with default 1; Core global admission and leases still cap actual concurrent execution |
| Random duration/score repeats forever for one Task or changes on retry | Donors sample per user execution; current Fanyuchang score is clamped Gaussian and Auto_WeLearn score is uniform | Derive both evidenced distributions from Core's immutable Execution ID plus Task/remote binding; never use retry-varying correlation IDs |
| Auto aggregate settings are flattened into one unexplained minute value or sampled independently per child | Modular Auto freezes one `configured 1..=300 + signed offset within 0..=30`, clamps once, and only then divides across the complete visible membership | Carry `WellearnAutoDurationBudget` with base/range/frozen offset/actual aggregate in parent authority. Main must add parent-batch runtime scope, entropy and persistence before exposing these settings; per-Task random seconds are a different donor flow |
| Unit/all-task batch membership changes between selection and execution | Donors allow one, multiple or every Unit; their hidden/completed filters differ, and Auto_WeLearn samples one aggregate minute budget then assigns `floor(total_seconds / visible_count)` to every selected child | Core must persist donor-flow, selected membership, selection-time visibility, sampled aggregate and every derived child target; call Provider fresh child rebind by frozen ordinal before execution, keep response-order movement from reordering the plan, re-evaluate current donor eligibility without adding children, and retain Auto's discarded remainder |
| Auto account CSV/TXT or batch queue is implemented inside WELearn Provider | Those features coordinate credentials, accounts and workers but add no WELearn request/response semantics | Keep them in shared Core/API/UI ownership, require secure credential import/export policy, and never copy donor plaintext password export into Provider code |
| SCO score/completion is promoted to answer evidence or used to infer retake/pass rules | No audited WELearn donor exposes Questions, per-Question grading, official answers, threshold semantics or retake creation | Keep the answer corpus/bootstrap/Strict Completion/Score Improvement integration unsupported for WELearn until exact donor or sanitized live evidence exists; a CMI score alone is not answer evidence |
| Verified Pending/InProgress after DurationReport is labeled `DurationInsufficient` | The readback proves only changed time plus preserved completion/progress/score; WELearn exposes no required/remaining duration or other incomplete-reason field | Leave `completion_diagnosis` as `None` so Core uses `RemoteUnknown`; add a specific diagnosis only after sanitized protocol evidence distinguishes duration, children, prerequisites, review, limits or windows |
| One selection shape is reused for every donor flow | Fanyuchang accepts ordered comma-separated Units, modular Auto checkboxes emit a response-order subset, while YZBRH and Auto single-file accept one Unit or all | Provider validates the flow-specific shape before membership filtering; Core must persist that exact all/explicit selection rather than normalize it to an unordered set |
| Unit visibility is incorrectly merged into donor batch filtering | YZBRH and Auto workers test only each SCO's `isvisible`; selected hidden Units can still contain missing/true leaf visibility | Preserve Unit, optional raw SCO and merged visibility separately; use raw SCO false only for the YZBRH/Auto filtering branches while retaining merged `NotOpen` for honest remote state |
| A restored batch plan mixes fields from different flow or membership snapshots | Dispatch, execution/atomic profiles, Unit/entry facts and Auto's aggregate division are frozen as one immutable donor attempt | Validate the complete Provider plan at construction and before every fresh child rebind; fail `Internal` rather than repairing profiles, reordering children or redistributing targets |
| Process restart reconstructs batch membership from child artifacts or a fresh scan | Child artifacts bind only one ordinal/target, while a rescan may add, remove or reorder siblings and redistribute Auto's aggregate | Persist the bounded `welearn.batch-plan.v1` snapshot alongside parent authority and every child artifact; deny unknown/version/derived-field drift and rerun full plan integrity on decode without rescanning or repairing membership |
| Recovery combines a valid parent authority, batch snapshot and child artifact from different attempts | Each value can validate independently while disagreeing on Course, flow, Unit selection, aggregate, expected Task, ordinal or target | Restore all three through the Provider's joint durable-artifact boundary, then validate the exact sequence records before fresh I/O; reject any cross-value mismatch before Task discovery, and never grant mutation authority |
| Fresh TaskDetail combines public Task facts with a different nested snapshot | Execution authority and exact Course/Unit/SCO route observations must come from one complete fresh inventory result | Require identical public and nested normalized Task values at the shared execution rebind boundary; reject mixed injected/restored details as `ProtocolDrift` before mutation |
| Fresh TaskDetail carries a stale fingerprint for changed normalized facts | The versioned Task fingerprint is derived from the complete sanitized Course/Unit/SCO observation | Recompute the inventory fingerprint at the shared execution rebind boundary and reject stale or substituted normalized snapshots before trusting route or membership facts |
| Fresh TaskDetail public state contradicts its normalized observations | Inventory derives visible Tasks from completion and maps every hidden Unit/SCO to `NotOpen` without discarding its capabilities | Reproject `remote_state` from the fingerprint-bound visibility/completion facts at execution rebind and reject contradictory details before transport |
| Fresh TaskDetail drops or invents Task capabilities | Audited WELearn SCOs expose only ProgressRead, ResourceExecution, ExecutionVerify, DurationRead and DurationReport; donors expose no Question/Answer/Submission/Browser Task protocol | Own one exact capability set in Task inventory and require it intact at execution rebind, rejecting missing, duplicate or extra capabilities before transport |
| Session challenge appears after password failures | Current donor reports captcha/SMS branches, including `code=0` with a non-empty `extraCheck.vcToken`, but supplies no automated solver protocol | Give the SMS token precedence over success redirect parsing, stop native retries and classify typed `HumanRequired`; the selected Capture recipe accepts and validates the browser-obtained Cookie |
| Browser SSO login choices or challenge fields change | Current browser implementation uses image `icode/sign`, SMS `vcode/vcToken`; the 2026-08-13 public challenge redirects lead to exact QQ/WeChat/Apple origins | Keep the five audited navigation origins exact and capture only the final WELearn Cookie; fail closed on a new top-level origin until a sanitized public/live audit updates the recipe |
| External browser OAuth is mislabeled as generic assisted login or callback-only exchange | Current SSO page exposes QQ/WeChat/Apple navigation, but the resulting authenticated state is a browser Cookie and no donor exchanges the final URL independently | Keep immutable Capture v4 assisted and v5 external-browser-OAuth recipes, validate the same WELearn Cookie/readiness, and leave callback exchange unset until protocol evidence exists |
| Anonymous prelogin Cookie is mistaken for authenticated readiness | Public prelogin already sets `acw_tc` and `ASP.NET_SessionId`; an anonymous session can issue the exact Course-list GET and receive `200` login HTML | Recipe v4 requires a current-loader `200 application/json` response for that exact route; native Course-list parsing remains the final acceptance authority |
| Expired sessions return either redirects or `200` login HTML | Donors and web stacks vary in expiry presentation | Detect only structural login pages, renew a native Composite once, restart the complete read and never retry other error kinds as authentication |

## Live-validation gate

Before `VerificationLevel` can advance:

1. validate password login, Cookie persistence, expiry and renewal;
2. compare Course/Unit/SCO inventory with the visible account;
3. record completion, progress, session time and total time for not-started,
   in-progress and completed SCOs;
4. exercise preservation-mode start → keep → finalize on an authorized resource;
5. exercise direct start → setscoinfo → save with one explicit score and time-mode setting;
6. re-read CMI and Course/SCO facts after each mutation and confirm the exact
   completion/progress/score or preservation predicate;
7. record one challenge, auth expiry or protocol-error branch and capture its
   sanitized browser/runtime requirements if present;
8. save only sanitized fixtures and a dated verification record.
