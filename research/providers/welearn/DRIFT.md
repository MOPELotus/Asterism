# WELearn drift register

| Risk | Static evidence | Required response |
|---|---|---|
| OIDC authorize/return shape changes | 2025 and 2026 donors construct/extract `rturl` differently | Parse redirects structurally, allowlist hosts and fixture each observed variant |
| OIDC introduces another trusted host or redirect count grows | The audited flow currently stays on two hosts and completes within a short chain | Fail closed at the host/redirect bound; expand only from sanitized current evidence |
| SSO Cookie domain/path layout changes | Current native boundary accepts only three audited domains and applies path scoping | Treat rejected scope or missing authenticated Course read as protocol drift, never relax from an error body alone |
| Course path alternates between `/student` and `/2019/student` | Donors mix both paths | Treat one as current only after live comparison; do not scatter hard-coded paths |
| `courseunits` accepts GET and POST in different donors | The current donor uses POST while YZBRH and Auto_WeLearn use GET | Prefer POST and retry the read-only GET variant only after an explicit 404/405; never broaden fallback to authentication, network or parser errors |
| Completion labels are localized or schema-dependent | `scoLeaves` donors inspect text fields directly | Keep raw sanitized label and map unknown values to `Unknown` |
| SCO module label is mistaken for assessment nature | Current inventory exposes Resource-like leaves but no formal/routine classification field | Keep assessment class `Unknown`; classify only from a separately audited explicit fact |
| `getscoinfo_v7` outer or nested shape changes | Audited response stores JSON text inside the outer `comment` string, reports integer `ret=0` and may omit `cmi` before first start; YZBRH checks the raw response for exact `学习数据不正确` text before parsing or starting | Keep both JSON layers bounded for reads. The CMI reader may retain that exact bounded marker despite an unexpected media type only after rejecting structural login HTML; only mutation-capable DurationReport consumes it as an initialization signal, read capabilities never start, and post-start CMI remains strict |
| Post-start or verification CMI omits preservation fields | Duration finalization requires explicit completion/progress/score/success and time observations | Stop before heartbeat/finalization when the baseline is incomplete; reject incomplete readback instead of comparing synthesized defaults |
| CMI progress grammar changes | Current donor reads decimal `progress_measure` in the `0..1` range | Accept only the bounded audited decimal grammar; add sanitized evidence before widening it |
| CMI time units/format drift | Current donors advance integer `session_time`/`total_time` against real seconds, but live variants may differ | DurationRead accepts only bounded canonical integer seconds and fails closed on every other grammar; widen only from sanitized evidence |
| Heartbeat semantics drift | Current Fanyuchang sends the long action every second; YZBRH and Auto send network keeps every 60 seconds | Expose only the evidenced `{1,60}` Master-controlled values and bind client-counter to 1 while preserve-fresh/implicit-server require 60; widen only from sanitized evidence |
| Distinct duration donors collapse into one heartbeat payload | YZBRH sends an initial preserved-state keep and preserve save; current Fanyuchang sends per-second client counters; Auto waits before implicit keeps and emits a bare save | Keep three explicit wire modes, bind current/Auto to their evidenced intervals, and fresh-read the same preservation predicate after every mode |
| Start/keep/save result grammar changes | Audited donors treat start/save `ret=0` and keep `ret=0/1` as accepted | Fail closed on missing, string or unknown result values; widen only from sanitized current evidence |
| Distinct donor mutation forms collapse into one request | Current Fanyuchang uses the query-uid endpoint, full start identity and the simple Referer across the lifecycle; YZBRH/Auto use the plain endpoint, minimal start identity and their exercise paths use a task-specific Referer | Keep the two ResourceExecution profiles explicit, infer the exact duration endpoint/start payload from its wire mode, and fixture both bounded endpoint/field sets |
| Session expires or a network result becomes uncertain after a duration mutation | Replaying start/keep/save could double-report time | Provider never renews-and-replays a mutation; Engine sends every only-DurationReport error or abandoned recovery directly to HumanRequired without retrying or consulting completion progress |
| Completion/progress/score can be changed independently of duration | All pinned donors implement direct `setscoinfo` and/or save payloads | Keep it as independent ResourceExecution; fresh-rebind, freeze selected score, never replay writes, and require exact fresh CMI verification |
| Historical CMI envelope marker is silently discarded | Current Fanyuchang sends plain JSON; YZBRH and Auto_WeLearn append `[INTERACTIONINFO]` to `setscoinfo.data` | Keep both formats explicit and fixture the exact suffix; do not infer or strip it from unrelated response parsing |
| Auto duration completion is forced through `setscoinfo` | Auto_WeLearn's duration worker uses only start/keeps/bare save or start/keeps/completed save, unlike its exercise worker | Keep `set_then_save` and `save_only` explicit; the modular completed-save route must execute inside the atomic duration-completion boundary and end with exact fresh CMI verification |
| One explicit negative mutation receipt suppresses later donor writes | Completion donors send later independent save calls even when start/set returns a well-formed non-success result | Record acceptance per call and continue only after an unambiguous integer result; any malformed/network/auth ambiguity stops with no replay, and only fresh exact CMI proves success |
| Historical duration receipt is treated as a hard success gate | YZBRH and Auto_WeLearn ignore well-formed negative start/keep/save results and continue, while current Fanyuchang gates start and stops its keep loop | Preserve the selected donor control flow, record accepted/rejected receipt counts, stop every ambiguous result, and require the same strict fresh CMI verification regardless of receipt |
| Historical duration request gaps or fields are replaced by the current donor form | YZBRH sleeps two seconds around baseline/start/first keep/final save and omits `timelimitsec/endcaltime`; current Fanyuchang sends those two fields in its per-second keeps | Keep fixed YZBRH request delays outside reported duration, bind keep fields to the selected mode, and expose sanitized delay/receipt metadata |
| Current donor's second save is treated as redundant and omitted | Fanyuchang always sends selected score and then another `crate=100` save; YZBRH/Auto stop after selected score | Keep both sequences explicit. The current-donor sequence verifies final score 100, and an ambiguous second save is never replayed |
| Completion erases a preceding duration report | Normal donor completion writes zero time, while current Fanyuchang's duration-then-completion flow carries its accumulated `session_time`/`total_time` into `setscoinfo` | Keep zero-time and preserve-fresh-time modes explicit. Fresh-time mode requires both baseline values before mutation and compares them with the immediate readback |
| Recovery accidentally reduces the frozen goal to generic Completed | Score is Provider-specific and absent from generic `RemoteProgress` | Use goal-bound `TaskExecutionCapability::verify_execution` with the frozen settings; rebind Task identity and fresh-read exact completion/progress/score without entering any mutation path |
| A composite Provider call loses the fresh-time completion facts | Current Fanyuchang carries accumulated `session_time`/`total_time` into final `setscoinfo` | Keep the fresh-time setting explicit, but do not use immutable plan context as mutation authority; the atomic Core boundary must carry and verify those values within the one donor operation |
| Atomic donor duration-completion is approximated by two singleton sessions | Current Fanyuchang starts once before client-counter keeps and final set/save; modular Auto starts once before implicit keeps and its completion-bearing save | Provider freezes `AtomicDurationCompletion` and both capability requirements but does not cross singleton authority. Core must persist/authorize/recover the atomic operation; never insert Auto's bare legacy save or a second start between phases |
| Atomic final mutation reuses the wrong standalone completion profile | Fanyuchang duration performs fresh-time set plus one score-100 save, while its standalone completion may do selected-score plus a second 100 save; modular Auto uses one zero-time score-0 save with no set | Freeze the exact `WellearnAtomicCompletionProfile` with the batch flow and do not resolve the final mutation from unrelated mutable defaults |
| Hidden SCO capability is discarded because inventory reports NotOpen | Current Fanyuchang explicitly disables its visibility skip and attempts every returned SCO | Preserve NotOpen as the remote observation while advertising the donor-evidenced mutation capabilities; fresh-rebind the exact Course/SCO and grant only exact ResourceExecution/DurationReport action sets a shared state exception |
| WELearn concurrency is capped below the donor control | Auto_WeLearn exposes 1–100 workers and applies the selected bound to its thread pool | Keep Provider/account settings bounded at 100 with default 1; Core global admission and leases still cap actual concurrent execution |
| Random duration/score repeats forever for one Task or changes on retry | Donors sample per user execution; current Fanyuchang score is clamped Gaussian and Auto_WeLearn score is uniform | Derive both evidenced distributions from Core's immutable Execution ID plus Task/remote binding; never use retry-varying correlation IDs |
| Unit/all-task batch membership changes between selection and execution | Donors allow one, multiple or every Unit; their hidden/completed filters differ, and Auto_WeLearn samples one aggregate minute budget then assigns `floor(total_seconds / visible_count)` to every selected child | Core must persist donor-flow, selected membership, selection-time visibility, sampled aggregate and every derived child target; call Provider fresh child rebind by frozen ordinal before execution, keep response-order movement from reordering the plan, re-evaluate current donor eligibility without adding children, and retain Auto's discarded remainder |
| One selection shape is reused for every donor flow | Fanyuchang accepts ordered comma-separated Units, modular Auto checkboxes emit a response-order subset, while YZBRH and Auto single-file accept one Unit or all | Provider validates the flow-specific shape before membership filtering; Core must persist that exact all/explicit selection rather than normalize it to an unordered set |
| Unit visibility is incorrectly merged into donor batch filtering | YZBRH and Auto workers test only each SCO's `isvisible`; selected hidden Units can still contain missing/true leaf visibility | Preserve Unit, optional raw SCO and merged visibility separately; use raw SCO false only for the YZBRH/Auto filtering branches while retaining merged `NotOpen` for honest remote state |
| Session challenge appears after password failures | Current donor reports captcha/SMS branches, including `code=0` with a non-empty `extraCheck.vcToken`, but supplies no automated solver protocol | Give the SMS token precedence over success redirect parsing, stop native retries and classify typed `HumanRequired`; the selected Capture recipe accepts and validates the browser-obtained Cookie |
| Browser SSO login choices or challenge fields change | Current browser implementation uses image `icode/sign`, SMS `vcode/vcToken`; the 2026-08-13 public challenge redirects lead to exact QQ/WeChat/Apple origins | Keep the five audited navigation origins exact and capture only the final WELearn Cookie; fail closed on a new top-level origin until a sanitized public/live audit updates the recipe |
| External browser OAuth is mislabeled as generic assisted login or callback-only exchange | Current SSO page exposes QQ/WeChat/Apple navigation, but the resulting authenticated state is a browser Cookie and no donor exchanges the final URL independently | Keep immutable Capture v4 assisted and v5 external-browser-OAuth recipes, validate the same WELearn Cookie/readiness, and leave callback exchange unset until protocol evidence exists |
| Anonymous prelogin Cookie is mistaken for authenticated readiness | Public prelogin already sets `acw_tc` and `ASP.NET_SessionId`; an anonymous session can issue the exact Course-list GET and receive `200` login HTML | Recipe v4 requires a current-loader `200 application/json` response for that exact route; native Course-list parsing remains the final acceptance authority |
| Expired sessions return either redirects or `200` login HTML | Donors and web stacks vary in expiry presentation | Detect only structural login pages, renew a native Composite once, restart the complete read and never retry other error kinds as authentication |

## Live-validation gate

Before `VerificationLevel` can advance:

1. validate password login, Cookie persistence, expiry and renewal;
2. compare Course/Unit/SCO inventory with the visible account;
3. record completion, progress, session time and total time for not-started,
   in-progress and completed SCOs;
4. exercise preservation-mode start → keep → finalize on an authorized resource;
5. exercise direct start → setscoinfo → save with one explicit score and time-mode setting;
6. re-read CMI and Course/SCO facts after each mutation and confirm the exact
   completion/progress/score or preservation predicate;
7. record one challenge, auth expiry or protocol-error branch and capture its
   sanitized browser/runtime requirements if present;
8. save only sanitized fixtures and a dated verification record.
