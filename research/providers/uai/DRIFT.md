# UAI drift register

| Risk | Static evidence | Required response |
|---|---|---|
| Login result/code shape changes | Current donors use string code `0`; captcha uses `1506` | Parse typed bounded envelopes; classify challenges as `HumanRequired` and never log bodies |
| JWT header format changes | Donors pass the returned JWT directly as `Authorization` | Validate through user-info/Course reads; do not guess a Bearer prefix |
| Login/user-info routes redirect or return HTML | Shared client does not follow redirects and both routes are audited JSON APIs | Classify redirects/404 as protocol drift and reject missing/non-JSON Content-Type before parsing |
| Read responses omit or change their success envelope | Audited UAI management APIs require numeric code `1` plus `success=true`; ucontent tree/progress/content require numeric code `0` | Require each route's exact success markers before consuming nested data; never accept a merely well-shaped body |
| JWT/openid are stored or renewed separately | Both values are required by different authenticated routes | Persist one strict ProviderCompositeSession and replace it atomically |
| Core rejects a native Password candidate before Provider validation | Password input uses a transient `ProviderSpecific` bundle but successful validation persists a `Composite` session | Declare `ProviderSpecific`, `Composite` and `Jwt` in Provider metadata; keep the candidate and replacement kinds distinct and require both to pass Core's metadata gate |
| Imported/native session metadata is confused | All paths store the same encrypted document with different lifecycle authority | Bind ManualImport, CaptureTool and BrowserExtension to Jwt and NativeProviderLogin to Composite; reject every other pairing and never renew imported sessions |
| Concurrent renewals overwrite a fresher session | Password credentials and composite session form one lifecycle set | Serialize by account, validate the new JWT, then compare-and-replace all three credentials atomically |
| JWT expiry is ignored or trusted as authentication | UAI supplies a JWT but user-info remains the native validation authority | Decode standard `exp` only as an early lifecycle/cache bound; never derive identity or validity from unsigned local claims |
| Expired JWT metadata prevents Password renewal | Core persists the same native-session expiry on the complete renewable credential set | Permit expired native Composite metadata only inside the exact username/password/composite renewal path, then user-info validate and atomically replace it |
| Unverified UAI becomes active accidentally | Native boundaries exist but have not passed live validation | Keep daemon registration disabled by default, require an independent explicit opt-in plus SecretStore and emit a Development warning |
| Course list and detail disagree on instance fields | List has `instanceId`; detail has `courseInstanceId` | Use stable CourseResource ID and refresh/bind detail before tree reads |
| A donor-local Course-code heuristic is mistaken for platform title authority | Rust donor `525c7ecf` expands `course_id` fragments into a small hard-coded Chinese display-name table without changing any UAI response | Keep bounded Course and CourseResource `name` fields authoritative; do not infer title, edition or book number from the opaque instance route |
| Course tree outer envelope changes | Current response stores JSON text in `course` | Bound both layers and fail closed on non-string, malformed or oversized nested data |
| New tree role appears | Audited roles are Unit, Section, Node, Link and Group | Reject unknown roles and add a sanitized fixture before mapping them |
| Group ID disappears or becomes URL-only | Historical parser falls back to URLs for generic nodes | Require explicit bounded Group IDs; never persist route URLs as Task identity |
| Persisted Group facts become stale before a read | CourseResource detail supplies a fresh instance route and trees can change | TaskDetail re-lists CourseResources and rebuilds the exact Group; missing facts become RemoteChanged |
| Task-detail fingerprints change shape silently | Core requires versioned fingerprints for fresh detail validation | Prefix normalized Group hashes with `v1:` and bump the version when fingerprint material changes |
| Completion flags gain new values | Current donor requires `pass == pass2 == perm == 1` | Keep each flag independent and map only sanitized verified combinations |
| Progress strategy is missing, malformed or detached from the tree | Current Rust donor carries per-Group required/minimum-score/time-window/statistic facts in each per-Unit progress response | Fetch exactly one progress document for every fresh tree Unit, reject missing/extra Units or invalid field types/ranges, merge before fingerprinting and preserve donor defaults only when the complete strategy object is absent |
| Per-Unit progress belongs to another account or Course snapshot | Both backend donors show `open_id`, `tutorialId`, `unit_id` and `publish_version` beside the leaf map | Before exposing a native document, require exact session-openid, fresh Course-instance and Unit-route echoes; retain only a valid optional publish version and reject any disagreement with Course progress or the tree |
| Course progress and Course tree disagree on Unit set or publish version | Current Rust donor independently reads Course progress before Unit progress, takes Unit keys and the current submit publish version from that response, while the tree may also expose a version | Read both fresh documents, require exact top-level Unit-set equality, fill a missing tree version from Course progress, reject conflicting versions and fingerprint separate Course-Unit strategy facts before any Task is returned |
| A mutation runs outside the donor availability window | Current Rust progress exposes start/end epoch seconds and the MIT runner acts only under the strict nonzero `start < now < end` rule | Normalize either zero endpoint as unbounded, require positive ordered endpoints otherwise, then re-read exact Unit/Group progress and reject every ResourceExecution or SubmissionExecute outside the fresh window |
| Annotator token claims/signature change | Both backend donors independently use the same HS256 openid claims and millisecond expiry across content, progress and submission routes | Keep generation private to the native transport, pin an exact-time vector and fail closed on header/clock errors |
| Native submit unexpectedly depends on an SSO browser cookie or browser-only headers | The MIT donor submits through its login-populated cookie jar, the Apache donor keeps session cookies and JWT defaults, and the current Rust donor imports cookie, openid/CSRF, app/platform and optional `u-school`; none proves JWT-plus-annotator alone in a clean client | Capture one exact `ucontent` Cookie plus optional `u-school` from the same bounded request snapshot, retain school inside the composite rather than as an access token, inject both only into `ucontent`, keep submission at Development and validate native/fallback transports independently |
| Study-record duration unit/meaning drifts | Frozen MIT donor explicitly documents Course/Unit/Task `duration` in seconds | Keep the study-record route independent, bind exact CourseResource/Unit/Group identities and recheck with live before/after measurements |
| `unitTaskSituation` is reduced to duration and loses Task policy/scoring facts | MIT's exact Task node also carries `finishProgress`, `required`, `scoreTaskFlag` and optional `taskQuesTotalScore` | Parse one bounded Provider-private Task study record and project only its seconds into DurationRead; keep completion, required and scoring facts distinct for a future shared contract |
| Course/Unit aggregate progress is collapsed into Task progress or accepted for another user | MIT donor exposes independent Course total and Unit finish-progress/duration/score/required facts keyed by CourseResource plus appUserId | Keep a separate bounded snapshot, bind fresh user-info and echoed appUserId, require unique `role=unit` IDs and finite `0..=100` progress/score; expose only through the dedicated shared capability once Core lands |
| Required-Course policy is confused with a progress leaf or fetched using stale strategy state | MIT donor refreshes Course detail, POSTs its strategy ID plus CourseResource and selects ordered per-Unit required Task IDs from the independent response | Keep a separate bounded policy owner, fresh-bind both request identities, require response Course/Unit strategy consistency, unique Unit/sort/Task IDs and typed score/unlock/time fields; reconcile with progress-leaf required only in the future shared policy contract |
| Progress duration is confused with study duration | Signed progress response also exposes an untyped numeric `duration` | Preserve progress value as raw only; populate seconds exclusively from the independent study-record response |
| Study-record tree duplicates/changes roles, or its Course route identity is dropped after parsing | Donor indexes nodeId and currently emits Unit/Section/Node/Link hierarchy while the request separately selects CourseResource and Unit | Require bounded CourseResource/Unit/Group route components, retain the complete normalized Task identity in the returned owner, require unique node IDs/audited roles/one exact matching Task and reject drift before returning the record or duration projection |
| Page residence stops producing duration | Current userscript relies on rendered page lifecycle, DOM/iframe clicks and optional media state | Prefer native network evidence when captured; otherwise use an account/Task-isolated rendered BrowserBridge plan and require fresh DurationRead after the residence budget |
| A fresh-looking duration response is attached to another browser leaf or predates its terminal result | Browser residence and `unitTaskSituation` are independent channels, and a transport read alone does not carry the Browser exchange identity | Bind the exact CourseResource/Unit/Group study record to the terminal checkpoint's batch/Browser-plan/result digests and require its observation time at or after exchange completion; preserve the observed fact without inventing a success delta |
| Browser DOM family changes | Frozen AutoPlayer `5.2.14` supports legacy `pc-slider`, Ant Tree/Menu and current `u3menu` plus two Tab families | Use plan v2 with exact ordered iframe/Tab/Task/popup/video selector equality, cap candidates/retries and fail with protocol drift when no uniquely bound visible leaf exists; never accept merely non-empty replacement selectors or arbitrary page-wide clicks |
| A helper treats command JSON or its own echo as selector/script authority | The audited donor uses broad page selectors and wildcard messages, while Core dispatch supplies only encrypted Provider command bytes plus actual browser transport facts | Consume and hash-check Core `SecretValue`, strictly validate every JSON key and wire/session/origin/frame/sequence/handle bound, then project only eight typed actions plus the compile-time read-only DOM profile. Keep BrowserSessionSpec read sources empty, reject command selector/script fields, restrict `ResidenceControl` to a selector-free bound runtime recipe and perform no DOM work at the Provider decode boundary |
| Capture widens a validated helper action with runtime selectors, script or a different DOM flow | The donor uses four distinct Menu discovery algorithms, scope-specific active tests, a fixed mouse-event order and bounded popup/video polling; an action enum alone does not describe those semantics | Compile only from the validated projection into a closed typed DOM recipe with private compile-time selector/text/rule fields. Revalidate exact equality and action bounds, redact Debug, keep Ping DOM-free and reject every recipe mutation before Capture interpretation; never accept a caller recipe, selector or script |
| Helper result JSON echoes foreign bindings, invents opaque handles or returns an observation for another action | Donor JavaScript constructs result objects after DOM work, but those objects cannot be trusted to repeat Core session/Task authority | Accept only action-specific bounded typed observations, retain and Debug-redact all projection bindings, derive handles and exact event/residence envelopes inside UAI, then pass the zeroizing encoded document through the existing fresh plan/command parser. Reject raw JSON, reordered ordinals, mismatched acknowledgements and residence/video/count overflow before Core result transport |
| Iframe message is accepted from the wrong page or session | Donor's iframe side exchanges `UAI_CMD`, menu and click-result events with wildcard `postMessage` targets, while the top page tries four ordered frame selectors and waits through a 30-second observer plus 20×1.5-second retries | Do not copy the wildcard trust model; freeze those timing/lookup bounds, require expected `ipub` origin, frame/window, account/Task isolation key and per-session nonce on every command/result, and reject a generic fallback frame unless transport observes the exact origin |
| A command is recovered with a foreign/downgraded cursor sidecar or the cursor is exposed to helper dispatch | The accumulated cursor contains Provider-private handles, labels, prior-result authority and the exact next-command digest; generic Core runtime state is opaque | Use stable type `uai.browser.cursor.v4`, atomically persist command and independent encrypted sidecar under identical session/sequence/issued-at metadata, hash-check both consuming secrets before fresh reads, recover command first then cursor, and dispatch command bytes only |
| Decrypted result-inbox bytes are copied into a non-zeroizing String before Provider validation | Raw browser events/results contain the session nonce, opaque handles and rendered labels | Move Core's `SecretValue` directly into the bounded Provider event/residence document, validate UTF-8 over borrowed secret bytes, hash and parse the same buffer, redact Debug and zeroize every success/error path |
| Core disposition or an open-ended result type is used to parse a result without recovering the exact UAI cursor stage | Intermediate and ExecutionTerminal distinguish orchestration, but do not themselves prove the command, cursor, Task or leaf accounting | Declare exact singleton Intermediate/ExecutionTerminal sets with no credential result, require registry set/classifier/inbox agreement, then consume exact result metadata/secret bytes with the persisted command and independent cursor sidecar. Fresh-rebind Task/settings/batch and dispatch only the recovered stage's typed transition. Reduce residence only to a non-resumable checkpoint requiring fresh DurationRead; never create a next command from ExecutionTerminal |
| Shared workflow callback trusts the compact child hash projection as a serialized Course batch | The compact plan intentionally omits full ordered Task hierarchy so the 8192-Task donor boundary fits Core storage; cursor recovery still requires fresh membership | Treat workflow bytes only as frozen type/profile/digest expectations, fresh-read complete Course and ordered Tasks through Provider transports, rebuild every digest under frozen settings, then decode command/cursor. Intermediate uses only the shared contiguous-next constructor; terminal performs independent DurationRead and reports Unknown state rather than inferring completion |
| Raw result bytes are parsed without their durable inbox identity or timestamped by a later caller | Core stores helper bytes with independent result type/digest/session/sequence/received-at metadata before Provider parsing | Consume metadata and secret together, require the exact issued UAI exchange and result family plus digest/time ordering before fresh reads, and use only the bound `received_at` for exchange completion |
| Paused or video time is counted as ordinary residence | Donor stops its countdown while paused or while a current video is actively playing and restarts when the start index/budget changes | Persist a bounded residence cursor/budget in Core, make pause/restart explicit, bind each control and acknowledgement through its own recoverable command exchange, let Core own active-residence cancellation/timer sequencing and verify only through a fresh exact DurationRead |
| Course browser membership is sorted by remote ID instead of donor menu order | AutoPlayer uses the rendered source `menuList` order and applies `slice(startIdx)` before distributing the total budget | Preserve fresh nested Task-tree traversal order while separately rejecting duplicate Group IDs; freeze complete ordered Micro/Group membership and Course publish version before resolving start/restart |
| A bare restart ordinal selects a different Course Micro after drift | Donor rebuilds remaining jobs from the current rendered menu, while the single-Task command previously bounded only the integer | Resolve restart targets from the immutable full Course plan and bind ordinal to the selected Micro identity digest; fresh membership or label/order drift requires a new plan |
| Course budget is floored before runtime Tab/Task counts are known | Donor keeps fractional `total / Micros / Tabs / Tasks` and applies positive `Math.round` only in the final wait loop | Freeze a rational total-seconds/remaining-Micros share, apply bounded Tab/Task divisors at the final leaf and never discard an invented plan-time remainder |
| Runtime settings change during execution | Master can override Provider defaults at account and Task scope, including pure-study and direct-discussion wire modes | Core resolves one complete versioned snapshot before execution; native mutation and future BrowserBridge paths reject missing, unknown or stale-schema values and never reread mutable settings mid-attempt |
| A durable Course child is restored against another profile or reordered/stale Task inventory, or its 8192-Task membership is truncated to fit Core's 64 KiB artifact | Cursor state alone binds batch digests but does not carry the complete frozen start authority or every source Task fingerprint; direct full-list JSON exceeds the shared artifact ceiling | Keep the complete bounded child Provider-owned and project namespaced credential-free JSON with exact identity/start/batch facts plus ordered-task and canonical-child digests. Require an explicit nonzero Core-owned runtime-profile digest and exact owner Task under the selected start Micro, then reconstruct the complete ordered fingerprints from fresh Course/Task/settings inputs and compare the entire projection before building the first cursor; never reduce the donor batch limit |
| Direct submit completes without duration | Backend donors submit answers independently of page residence | Keep CompletionService and DurationService separate; never claim execution from completion alone |
| Preset completion is forced through ordinary retrying TaskExecution, one donor shape erases the other, or its final request has no exact pre-dispatch identity | Current Rust/MIT donors mark study resources with a no-Question `submitType=2` POST, Apache uses expanded `instanceId=0` placeholders, and `SubmissionDraft` requires at least one real Question | Advertise ResourceExecution only with ExecutionVerify+ProgressRead, freeze Master-resolved `marker|placeholder`, bind Apache's padded/truncated ordered types and fresh Course version, route every final body through a zeroizing exact URL/content-type/body request owner, execute once, and let Core recover through exact Group progress without replaying the POST; add shared TaskExecution digest registration rather than weakening the Provider boundary |
| Incomplete UAI verification guesses score, duration, prerequisite or child diagnosis from one zero completion flag | Donors define only all-three `pass=pass2=perm=1` as complete; no audited source assigns causal semantics to each incomplete combination, while the donor strategy independently defines a strict positive close time | Map only an incomplete exact ResourceExecution verification whose fresh close time is at or before the progress observation to `WindowClosed`; require the exact result schema/facts, ignore diagnosis after Completed, and leave every other cause `RemoteUnknown` |
| Direct discussion marker/placeholder is conflated with a verified reply workflow or selected from mutable state | Apache exposes a direct `submitType=2` marker, a `submitType=1` `instanceId=0` placeholder, and a topic/reply/readback then Group-completion sequence | Advertise direct ResourceExecution only for exact single discussion, freeze Master-resolved `marker|placeholder` in the versioned attempt settings, inject the fresh Course version into placeholder judges, and keep reply Draft/readback/second mutation independent; all paths use fresh task-leaf preflight and progress-only final verification |
| Subjective empty execution erases ordinary answer submission or is invoked implicitly | Apache's `SUBJECTIVE_ALLOW_EMPTY` sends the generic placeholder body for exact `short_answer`, while the same Task also has the normal Question/Answer/Submission flow | Advertise ResourceExecution/ExecutionVerify alongside, not instead of, SubmissionExecute/SubmissionVerify; require the caller to authorize the separate capability, fresh-bind the exact short-answer Task and publish version, send one bounded placeholder body, and verify only through fresh exact Group progress |
| Preset labels select the wrong mutation shape | Apache pads/truncates comma-split base types into ordered `instanceId=0` placeholders, while the current Rust donor uses the empty body only for fresh `tab_type=text|video` leaves | Treat the five base labels as candidates only; validate the frozen mode, expand placeholder types exactly from fresh Task detail, refresh exact Unit/Group progress, require text/video inside the donor window, skip already-completed leaves and reject task/missing/unknown tab types |
| Encrypted content/answer framing changes | Donors currently use `unipus.` hex AES-ECB envelopes with a key suffix | Bound and validate every layer before decrypting; zeroize decrypted bytes on success and failure, recursively zeroize raw parsed string values, and never persist ciphertext, key material or route context |
| Nested Question content changes between encoded text and inline object | Current Rust donor explicitly accepts both representations | Apply one size bound and zeroizing parsed owner to either representation; reject all other nested types and bind the resulting semantic fingerprint before answer resolution |
| Group `base` cardinality no longer describes its Questions | Donors use either one homogeneous type or one type per Question | Require a bounded positive `question_num` plus exactly one supported type or an exact type-to-count cardinality before advertising or parsing Questions |
| Multi-Question module order or cardinality drifts | Current Rust and MIT donors serialize one ordered `quesDatas` row per native module and flatten child completion/judge rows in that same order | Require immutable draft positions `1..n`, exact fresh `question_num`/type cardinality, unique positive numeric module IDs and an exact ordered user-module readback before confirming any Question |
| Multi-Question client-authored metadata diverges | Current Rust donor uses minimal `contextVersion=1`/`answerVersion=1`; MIT/Apache ordinary multi builders use `1/0` plus locally fabricated course-answer score maps, while the MIT single builder uses minimal `0/0`; Apache's dedicated empty-placeholder behavior explicitly depends on that map | Retain the proven ordinary `0/0` single and current-donor `1/1` multi bodies without adding unproven score state; preserve the separate donor-evidenced placeholder map only inside its dedicated ResourceExecution mode, always with fresh judge version and progress verification |
| Course publish version is dropped, stale or hard-coded in judge metadata | Current Rust donor reads Course progress `publish_version` and emits it as `thirdPartyJudges[].versions.course` with `answer=3`; the MIT compatibility builder independently emits `course=0`/`answer=0` | Obtain a bounded positive numeric or numeric-string version from fresh Course progress, cross-check any tree copy and select the complete current pair at execution; use the independently evidenced legacy pair only for an explicitly injected transport without Course progress, include the version in the Task fingerprint and reject invalid or changed shapes before mutation |
| Question judge labels are guessed from Group `base` | Current Rust donor copies each decrypted content child/module `type` and child `replyType`; those labels can differ from the tree base | Preserve only bounded sanitized judge labels during answer-free parsing, require an exact label per answer child before every mutation, and fail before POST when content lacks that current contract |
| Missing or non-native Question ID reaches a mutation | Standard-answer and both donors' submission protocols use the positive numeric answer-entry instance ID | Require an explicit bounded remote Question ID for reads and a positive numeric instance ID for execution; never submit a Group/position fallback |
| A Question snapshot is reused against another Group route | Native Question IDs and option labels are not sufficient to prove the CourseResource/Unit/Group target | Persist the stable remote Task identity in sanitized Question metadata and require an exact match before answer resolution or draft construction |
| Answer submission targets a stale, completed or non-task progress leaf | The answer route mutates one Unit/Group while current progress separately identifies task mode, completion and scheduling | Pass the immutable Unit identity into transport, refresh Course detail plus exact Unit progress immediately before POST, require an incomplete `tab_type=task` Group inside its window and fail before body construction on mismatch |
| Answer submission wire bytes are rebuilt after durable mutation identity should be frozen, or a media QuestionSession reaches the legacy unregistered mutation path | The exact Course instance and openid are discovered only at the final native boundary, while the immutable Draft/plan alone does not identify the complete HTTP request | After fresh route/account/progress rebinding, materialize one zeroizing request owner whose digest binds method, exact URL, content type and full body; for an artifact-bearing session first rebind the exact complete manifest, freeze the authenticated session and annotator material into a prepared `uai.answer-submit.v1` operation, persist its digest before dispatch and return a separate response digest/receipt. Send only the frozen owner's bytes, keep transport identity separate from Draft/plan semantics, redact every wire field from Debug and never replay an ambiguous issued operation; retain the legacy path only when Core claimed no artifact session |
| Submission throttling is mistaken for success | Donor returns `600001`/`600002` for rate limiting | Return typed retry state and never repeat a mutation without Core idempotency/recovery authority |
| An ambiguous mutation failure is retried inside the Provider | A timeout or body failure cannot prove whether the server accepted the request | Return the first Network error after one mutation attempt so Core enters receipt-independent verify-only recovery and never resubmits |
| Submit version cannot safely identify one user-module path | Submit acknowledgement supplies the version later combined with Group identity | Accept only a bounded ASCII path-component version before persisting the receipt; verification reuses the same validator |
| Submit receipt is accepted as result verification | Fresh user-module and progress reads are separate donor calls | Keep SubmissionExecute and SubmissionVerify separate and require a fresh identity-bound readback |
| A real scored submission is flattened to `score=None` | MIT checks receipt-versioned user-module `__SUMMARY__.answerList` for counted types `1/3` and exposes `score_avg`, including zero | Parse score only from the same Course/Group/version-bound readback, represent `0..=100` in fixed-point thousandths, preserve zero, and leave uncounted/absent summaries as `None`; never trust receipt score as verification |
| Receipt-bound user-module policy is discarded, partially accepted or treated as retake authority | The same exact ordinary, upload or compound result may carry `strategyId`, required/recording flags, `task_mini_score_pct`, availability times and submit-state timestamps; donors do not establish that recording flags reopen a completed Task | Preserve one result-digest-bound Provider-private policy owner only after the adapter's exact answer/object-key/oral readback and module-order checks succeed and when the complete typed policy is present, keep legacy all-absent responses compatible, reject partial/invalid material, and never infer retake or shared score precedence from it |
| A first-bind bootstrap guesses a user-module version or mutates a Task to reveal history | No donor exposes account-wide answer-history/version enumeration; MIT requires the accepted receipt version and the Rust helper is unused/pre-reserved with undocumented wall-clock suffix semantics | Permit only bounded read-only inventory and evidenced result routes, return a partial/unsupported harvest report, and never submit, reopen or guess a module version during bootstrap |
| User-module summary rows are aligned to Draft Questions by map order | `answerList` exposes per-child `done/right/student_answer`, while UAI multi-child modules are one shared Composite Question and the donor does not prove a one-row-per-Draft-item rule | Preserve the raw bounded protocol fact only behind the Provider boundary until Core/Domain can bind ordered child evidence; never promote overall score or positional guesses to per-Question correctness |
| Strict Completion and Score Improvement are collapsed into one retry loop | Fresh UAI completion requires all three progress flags, while all execution donors skip completed Groups and expose no reset/redo/retake authority | Let Strict Completion continue only on fresh incomplete/open Tasks; report retake unavailable/unknown after completion and never call ordinary submit on a completed Group |
| One UAI score threshold is treated as universal pass policy | Group `min_score_pct`, receipt-bound user-module `task_mini_score_pct`, Unit `passScore`/`scoreType`, Course `scoringMode` and study-record `scoreTaskFlag` are independent donor facts | Retain every typed fact with provenance and fail closed on precedence until a shared policy contract plus sanitized live evidence establishes reconciliation; the Provider-private user-module owner is not a precedence decision |
| Broad browser Menu discovery selects a video-player menu | AutoPlayer issue 17 reports that the generic `menu` selector can bind a player control rather than the Course hierarchy | Require the frozen hierarchy/cardinality/text checks and unique fresh Task match, classify ambiguous candidates as protocol drift, and add sanitized page-source/live fixtures before changing the selector profile |
| A missing receipt causes verification to guess the current module version | The readback route is keyed by the submit response version | Return Inconclusive without transport I/O when no accepted version receipt exists |
| User-module route and response identify different Courses or Groups | The route uses fresh `courseInstanceId` while response and state carry separate module/submit metadata | Bind fresh Course detail, top-level and submit-info Course, exact Group-version module, submit-info Group and both version fields before inspecting answers |
| User-module answer is stale, partial or changed | `quesData` embeds separately encoded context and answer children | Require the complete exact ordered Question set, submitted context for every row, every child `isDone`, and exact immutable draft-answer equality |
| UAI task kinds acquire the wrong generic mutation default | Donors branch across choice, text, fillblank/banked-cloze, ordering, matching, oral, discussion, upload, preset and composite behaviors | Use exact typed mappings and content-derived judge metadata; implement oral/discussion/upload/exit-ticket as dedicated families, and fail closed only on structural drift rather than using unknown types as an exclusion policy |
| Raw unknown protocol values are copied into durable drift diagnostics | Unknown roles, reply types and result rows may contain user or credential-shaped material | Persist only versioned bounded shape: JSON kind, presence/counts and SHA-256 plus byte length for the two explicitly approved label boundaries. Never retain raw responses, labels, answers, field-name lists, routes or credentials; leave other drift exits unobserved until their minimal shape is reviewed |
| `multiFileUpload` becomes unreachable after Task normalization | The platform/donor label is camel-case while ordinary UAI base labels are lowercase | Canonicalize case-insensitive upload matches back to exactly `multiFileUpload` at the Task-tree parser boundary so single and compound upload gates consume one stable label |
| Oral placeholder judges retain a captured Course version | Donor example bodies contain one concrete publish version, while current inventory independently refreshes the active Course version | Freeze the positive signed-range version from fresh TaskDetail and pass it through the oral execution boundary; reject missing/zero/oversized values before POST |
| Matching plus oral is split into two mutations, loses the oral instance, reproduces the donor's cross-index bug, or dispatches an unregistered body | Apache submits `basic-scoop-content,oral-sentence` as one body and derives both native instance slots from the same encrypted answer array; its single builder independently proves each child's `answersExtra` becomes that child's `extra`, while the compound helper accidentally indexes modules by child ordinal | Freeze the matching Draft, exact fresh two-module Task, ordinary/oral instances and each bounded oral child value/extra into one zeroizing Provider-private plan; derive only donor-stable judge encodings, materialize a separate exact POST/route/content-type/body request digest after fresh route/account rebinding, submit and verify the ordered pair atomically, and fail closed on unsupported object/non-text-list shapes rather than either dropping the capability or copying the bad index |
| Matching or ordering answers are flattened | `basic-scoop-content` and `sequence` retain ordered child semantics in content and answer documents | Bind matching-left text and ordering child cardinality in the immutable Question snapshot, then reconstruct the exact ordered answer children inside execution |
| A multi-child choice module is flattened into one option set | The current Rust donor solves and submits every module child independently, and child option labels commonly repeat | Represent the module as one Composite Question, bind each child kind/options and rebuild one answer child per component without merging repeated labels |
| `video-popup` is treated as a fixed type or an empty preset | MIT reads its standard-answer children and submits type 2; the current Rust donor derives behavior from decrypted module/child `replyType` | Advertise from the exact tree base, derive only audited choice/text shapes from fresh content, keep the non-empty answer/judge rows, select type 2 only for an all-`video-popup` plan and fail closed on incompatible child semantics |
| Discussion reply receipt is mistaken for Group completion, a reply page is accepted for the wrong topic, or either mutation is ambiguously replayed | Donor performs topic lookup/reply mutation and a separate Group completion mutation | Bind every reply page to its requested topic, freeze the zeroizing exact reply intent and pre-dispatch digest, require exact current-user/content readback before authorizing one fresh Task/progress-bound completion with its own digest, and keep final progress verification separate; shared Core must durably bind both attempts and never replay either ambiguous mutation |
| Upload paths or bytes escape Provider ownership, request identity is registered before fresh route/account resolution, a compound upload is split, or an upload receipt is treated as completion | Donor obtains a short-lived token/fileKey before object-store upload; a single upload submits `instanceId=0`, while mixed upload and choice answers share one Group mutation | Keep bytes/token/key/body in zeroizing Provider owners, materialize separate exact request digests only at each fresh CMS/Qiniu/single-final/compound-final boundary, bind Task fingerprint/Course/Unit/Group/module/artifact across all stages, fresh-rebind before final POST, reject upload-only execution for a compound Group, require receipt-versioned exact key readback, and still use fresh progress as the completion authority; Core supplies durable digest registration and the Artifact/Draft/attempt contract |
| Media routes disappear at the Question cache boundary, a partial snapshot consumes another Question's route, or AnswerResolve accepts a foreign session | Donors can transcribe answer media, while Domain intentionally excludes fetch URLs | Atomically aggregate the complete ordered Question manifest into one encrypted QuestionSession continuation whenever any item has media; bind Task/Group/every Question/position/content fingerprint and each ordered attachment set, then require exact Provider type/phase/revision/digest and recompute canonical URL, kind and attachment ID before session-aware AnswerResolve or any future downloader can receive a route |
| A media downloader forwards UAI credentials to an external host, follows a redirect, accepts unbounded bytes or attaches a response to another Question | The current donor's generic client carries its default authorization/cookie set even when a media URL changes host | Derive an immutable fetch authority only from the freshly rebound complete Question manifest; digest the exact Task/Question/fingerprint/attachment/GET route and response ceiling, select session credentials only for exact `ucontent.unipus.cn`, keep external hosts anonymous and reject every redirect/final-route change. After shared DNS/private-range and bounded streaming execution, consume the secret body only through the same plan, require exact status 200 plus nonempty in-bound bytes and bind its digest/length to the request digest before it can enter the AnswerResolve attempt |
| Downloaded subtitles lose their attachment/response binding, malformed text is silently repaired, or audio/video bytes are accepted as a transcript without model provenance | The current donor routes `.vtt`/`.srt` through its header/ordinal/timestamp line parser before the ffmpeg/Whisper branch | Parse only an accepted manifest-marked subtitle response, require bounded valid UTF-8 and nonempty normalized cues, bind the transcript digest to the exact response digest and keep both response and text zeroizing; reject non-subtitle responses while leaving audio/video transcription behind the shared executor/model identity contract |
| A multi-source Question silently drops, duplicates or reorders media before shared execution | The current donor iterates every module media source in captured order before joining nonempty transcriptions | Materialize the complete nonempty Question source set as one Provider-private batch, retain each exact request authority in manifest order, reject duplicate request digests and bind count plus ordered digests into a stable batch digest; leave retry/skip/result aggregation to the shared attempt policy |

## Live-validation gate

After WebUI and Asterism-Plugin/Yunzai are complete and read-only test accounts
are available, before advancing verification:

1. validate Password login, captcha classification, JWT/annotator acceptance,
   expiry and recovery;
2. compare CourseResource and Unit/Section/Micro/Group trees with the visible
   account;
3. record per-Group flags plus Course/Unit completion and duration before and
   after ordinary study;
4. Capture the actual heartbeat/telemetry lifecycle and validate the rendered
   BrowserBridge page-residence/interaction path in the first batch;
5. verify native, BrowserBridge and Capture-dependent execution with independent
   fresh completion and duration reads;
6. commit only sanitized fixtures and a dated verification record.
